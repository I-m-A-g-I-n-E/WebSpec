# ROADMAP-C

Index of every `TODO(C)` item — multi-tenant (tier C) work that is **located, not built**. Tier C
is the eventual public, many-user WebSpec service; tier B (what ships today) is the self-hostable
single-operator gateway. Every row below is Proposed (C): named and mapped to a spec section, but
not implemented. See the [status matrix](index.md#status) for the Implemented-vs-Proposed summary,
and the design doc itself —
[`docs/superpowers/specs/2026-07-11-webspec-roadmap-design.md`](superpowers/specs/2026-07-11-webspec-roadmap-design.md)
— for full detail (this file mirrors its §8 summary index).

| Item | Spec section |
|---|---|
| Pluggable `SecretBackend` (1Password Connect, other password managers, HashiCorp Vault) — B ships only the `op`-CLI backend | [§3.1 Auth model](superpowers/specs/2026-07-11-webspec-roadmap-design.md#31-auth-model-the-password-manager-is-the-session-authority) |
| Per-user / asymmetric guard keys (per-user vault items, or client-holds-private-key / gateway-holds-public-key Ed25519) so tenants can't sign for each other | [§3.1 Auth model](superpowers/specs/2026-07-11-webspec-roadmap-design.md#31-auth-model-the-password-manager-is-the-session-authority) |
| 1Password-issued bearer validated per-request (a "real" server-side session), instead of a shared symmetric secret | [§3.1 Auth model](superpowers/specs/2026-07-11-webspec-roadmap-design.md#31-auth-model-the-password-manager-is-the-session-authority) |
| Widen guard HMAC tag to ≥16 bytes, or move to Ed25519 signatures (current 4-byte definer tag is a tamper-evidence checksum, not the auth boundary — the guard HMAC's security rests on nonce + key secrecy) | [§3.2 Hardening pass, item 5](superpowers/specs/2026-07-11-webspec-roadmap-design.md#32-hardening-pass-each-item-a-test) |
| Multi-vector semantic search (canonical/predicate/object indices), per-user preference & history boosts, negative-example repulsion, query expansion | [§5.1 B1 — Semantic resolver](superpowers/specs/2026-07-11-webspec-roadmap-design.md#51-b1-semantic-resolver) — described today in `docs/discovery/*` |
| Provider billing/quota/liveness enrichment ("does this API key still have credit / an active account") — no generic shortcut, one adapter per provider | [§5.2 B2 — Keychain graph & secrets catalog](superpowers/specs/2026-07-11-webspec-roadmap-design.md#52-b2-keychain-graph-secrets-catalog) |
| OAuth/JWT identity, multi-tenant config & isolation, format suffixes, REST collection hierarchy, `DELETE` method — the aspirational `docs/url-grammar/*` and `docs/auth/*` surface, not needed for B | [§8 summary index](superpowers/specs/2026-07-11-webspec-roadmap-design.md#8-c-located-not-built-summary-index) |
| De-duplicate `docs/` vs `wiki/` — they are near-identical hand-maintained mirrors; make `wiki/` generated from `docs/` (or drop it) so there is a single source of truth. **Not done in this task** — `wiki/` is left untouched here and tracked as this follow-up. | [§6, item 5 ("§6.5")](superpowers/specs/2026-07-11-webspec-roadmap-design.md#6-docs-reconciliation-track-a-deliverable) |

## Notes

- Every C-tier extension point above should also carry a `# TODO(C): …` marker in the nearest
  relevant module, pointing back to this file (per §0 of the design doc: "C behavior ships as
  `# TODO(C): …` markers in code plus an entry in `docs/ROADMAP-C.md`").
- This file is generated by hand from §8 of the design doc; if §8 changes, update this table to
  match.
